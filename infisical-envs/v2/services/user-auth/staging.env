# ==============================================================================
# optima-user-auth / staging
# User Auth 服务 Staging 配置
# 共享引用已移至 common.env，此文件只包含 staging 特有配置
# ==============================================================================

# ================================================
# Database Configuration (Staging 特有)
# ================================================
DB_USER=${staging.shared-secrets.database-users.AUTH_DB_USER}
DB_PASSWORD=${staging.shared-secrets.database-users.AUTH_DB_PASSWORD}
DB_NAME=${staging.shared-secrets.database-names.AUTH_DB_NAME}
REDIS_DB=${staging.shared-secrets.redis-databases.REDIS_DB_AUTH}

# 组合 URL（完整 Infisical 引用）
DATABASE_URL=postgresql+asyncpg://${staging.shared-secrets.database-users.AUTH_DB_USER}:${staging.shared-secrets.database-users.AUTH_DB_PASSWORD}@${common.shared-secrets.infrastructure.DATABASE_HOST}:${common.shared-secrets.infrastructure.DATABASE_PORT}/${staging.shared-secrets.database-names.AUTH_DB_NAME}
REDIS_URL=redis://${common.shared-secrets.infrastructure.REDIS_HOST}:${common.shared-secrets.infrastructure.REDIS_PORT}/${staging.shared-secrets.redis-databases.REDIS_DB_AUTH}

# ================================================
# URLs (Staging 特有)
# ================================================
OAUTH_ISSUER=${staging.shared-secrets.domain-urls.AUTH_URL}
DEVICE_VERIFICATION_URI=${staging.shared-secrets.domain-urls.AI_URL}/device
NEXT_PUBLIC_AUTH_URL=${staging.shared-secrets.domain-urls.AUTH_URL}
NEXT_PUBLIC_API_BASE_URL=${staging.shared-secrets.domain-urls.AUTH_URL}/api/v1

# ================================================
# Application Configuration (Staging 特有)
# ================================================
JWT_SECRET_KEY=staging-JUV66bpWdIlXYX4FXStMaZV5CDeklKqDqlTj0YOeiE=
SECRET_KEY=staging-1JUV66bpWdIlXYX4FXStMaZV5CDeklKqDqlTj0YOeiE=
LOG_LEVEL=DEBUG
DEBUG=true
NODE_ENV=staging

# ================================================
# CORS Configuration (Staging 特有)
# ================================================
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8295","https://*.optima.chat","https://*.optima.shop","https://*.optima.onl","https://admin.stage.optima.onl","https://portal.admin.stage.optima.onl"]

# ================================================
# Frontend Configuration (Staging 特有)
# ================================================
NEXT_PUBLIC_CLIENT_ID=admin-panel
