# ==============================================================================
# optima-user-auth / prod
# User Auth 服务 Production 配置
# 共享引用已移至 common.env，此文件只包含 prod 特有配置
# ==============================================================================

# ================================================
# Database Configuration (Prod 特有)
# ================================================
DB_USER=${prod.shared-secrets.database-users.AUTH_DB_USER}
DB_PASSWORD=${prod.shared-secrets.database-users.AUTH_DB_PASSWORD}
DB_NAME=${prod.shared-secrets.database-names.AUTH_DB_NAME}
REDIS_DB=${prod.shared-secrets.redis-databases.REDIS_DB_AUTH}

# 组合 URL（完整 Infisical 引用）
DATABASE_URL=postgresql+asyncpg://${prod.shared-secrets.database-users.AUTH_DB_USER}:${prod.shared-secrets.database-users.AUTH_DB_PASSWORD}@${common.shared-secrets.infrastructure.DATABASE_HOST}:${common.shared-secrets.infrastructure.DATABASE_PORT}/${prod.shared-secrets.database-names.AUTH_DB_NAME}
REDIS_URL=redis://${common.shared-secrets.infrastructure.REDIS_HOST}:${common.shared-secrets.infrastructure.REDIS_PORT}/${prod.shared-secrets.redis-databases.REDIS_DB_AUTH}

# ================================================
# URLs (Prod 特有)
# ================================================
OAUTH_ISSUER=${prod.shared-secrets.domain-urls.AUTH_URL}
DEVICE_VERIFICATION_URI=${prod.shared-secrets.domain-urls.AI_URL}/device
NEXT_PUBLIC_AUTH_URL=${prod.shared-secrets.domain-urls.AUTH_URL}
NEXT_PUBLIC_API_BASE_URL=${prod.shared-secrets.domain-urls.AUTH_URL}/api/v1

# ================================================
# Application Configuration (Prod 特有)
# ================================================
JWT_SECRET_KEY=JUV66bpWdIlXYX4FXStMaZV5CDeklKqDqlTj0YOeiE=
SECRET_KEY=1JUV66bpWdIlXYX4FXStMaZV5CDeklKqDqlTj0YOeiE=
LOG_LEVEL=INFO
DEBUG=false
NODE_ENV=production

# ================================================
# CORS Configuration (Prod 特有)
# ================================================
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8295","https://*.optima.chat","https://*.optima.shop","https://*.optima.onl","https://admin.optima.shop","https://portal.admin.optima.shop"]

# ================================================
# Frontend Configuration (Prod 特有)
# ================================================
NEXT_PUBLIC_CLIENT_ID=admin-panel
