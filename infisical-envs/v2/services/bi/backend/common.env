# ==============================================================================
# optima-bi-backend / common
# BI Backend 服务跨环境共享配置
# ==============================================================================

# ================================================
# ClickHouse Configuration (引用 common/clickhouse)
# 注意: HOST/PORT 从 common 获取, USER/PASSWORD 从各环境的 staging/prod.env 覆盖
# ================================================
CLICKHOUSE_HOST=${common.shared-secrets.clickhouse.CLICKHOUSE_HOST}
CLICKHOUSE_PORT=${common.shared-secrets.clickhouse.CLICKHOUSE_PORT}

# ================================================
# Kafka Configuration (引用 common/clickhouse)
# ================================================
KAFKA_BOOTSTRAP_SERVERS=${common.shared-secrets.clickhouse.KAFKA_BOOTSTRAP_SERVERS}

# ================================================
# PostgreSQL (引用 common/infrastructure)
# ================================================
DATABASE_HOST=${common.shared-secrets.infrastructure.DATABASE_HOST}
DATABASE_PORT=${common.shared-secrets.infrastructure.DATABASE_PORT}

# ================================================
# Redis Configuration (引用 common/infrastructure)
# ================================================
REDIS_HOST=${common.shared-secrets.infrastructure.REDIS_HOST}
REDIS_PORT=${common.shared-secrets.infrastructure.REDIS_PORT}

# ================================================
# Static Configuration
# ================================================
CACHE_TTL_SECONDS=60
QUERY_TIMEOUT_MS=30000
MAX_ROWS_PER_QUERY=10000
