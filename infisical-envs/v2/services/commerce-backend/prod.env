# ==============================================================================
# optima-commerce-backend / prod
# Commerce Backend 服务 Production 配置
# 共享引用已移至 common.env，此文件只包含 prod 特有配置
# ==============================================================================

# ================================================
# Database Configuration (Prod 特有)
# ================================================
DB_USER=${prod.shared-secrets.database-users.COMMERCE_DB_USER}
DB_PASSWORD=${prod.shared-secrets.database-users.COMMERCE_DB_PASSWORD}
DB_NAME=${prod.shared-secrets.database-names.COMMERCE_DB_NAME}
REDIS_DB=${prod.shared-secrets.redis-databases.REDIS_DB_COMMERCE}

# 组合 URL（完整 Infisical 引用）
DATABASE_URL=postgresql://${prod.shared-secrets.database-users.COMMERCE_DB_USER}:${prod.shared-secrets.database-users.COMMERCE_DB_PASSWORD}@${common.shared-secrets.infrastructure.DATABASE_HOST}:${common.shared-secrets.infrastructure.DATABASE_PORT}/${prod.shared-secrets.database-names.COMMERCE_DB_NAME}
REDIS_URL=redis://${common.shared-secrets.infrastructure.REDIS_HOST}:${common.shared-secrets.infrastructure.REDIS_PORT}/${prod.shared-secrets.redis-databases.REDIS_DB_COMMERCE}

# ================================================
# S3 Buckets (Prod 特有)
# ================================================
MINIO_BUCKET=${prod.shared-secrets.s3-buckets.COMMERCE_S3_BUCKET}
MINIO_TEMP_BUCKET=${prod.shared-secrets.s3-buckets.COMMERCE_S3_TEMP_BUCKET}
MINIO_PUBLIC_DOMAIN=${prod.shared-secrets.s3-buckets.COMMERCE_S3_PUBLIC_DOMAIN}

# ================================================
# Stripe (Prod 特有)
# ================================================
STRIPE_SECRET_KEY=${prod.shared-secrets.stripe.STRIPE_SECRET_KEY}
STRIPE_CONNECT_CLIENT_ID=${prod.shared-secrets.stripe.STRIPE_CONNECT_CLIENT_ID}
STRIPE_CONNECT_WEBHOOK_SECRET=${prod.shared-secrets.stripe.STRIPE_CONNECT_WEBHOOK_SECRET}
STRIPE_WEBHOOK_SECRET=${prod.shared-secrets.stripe.STRIPE_WEBHOOK_SECRET}
STRIPE_PRICE_ID_ENTERPRISE_MONTHLY=${prod.shared-secrets.stripe.STRIPE_PRICE_ID_ENTERPRISE_MONTHLY}
STRIPE_PRICE_ID_ENTERPRISE_YEARLY=${prod.shared-secrets.stripe.STRIPE_PRICE_ID_ENTERPRISE_YEARLY}
STRIPE_PRICE_ID_PRO_MONTHLY=${prod.shared-secrets.stripe.STRIPE_PRICE_ID_PRO_MONTHLY}
STRIPE_PRICE_ID_PRO_YEARLY=${prod.shared-secrets.stripe.STRIPE_PRICE_ID_PRO_YEARLY}

# ================================================
# URLs (Prod 特有)
# ================================================
USER_AUTH_URL=${prod.shared-secrets.domain-urls.AUTH_URL}
API_BASE_URL=${prod.shared-secrets.domain-urls.API_URL}

# ================================================
# Application Configuration (Prod 特有)
# ================================================
SECRET_KEY=Loee027yH-7zkojJ_V57rBwiaB7jUUEjB9Q7KK9Tg1A
LOG_LEVEL=INFO
DEBUG=false

# ================================================
# OAuth Configuration (Prod 特有)
# ================================================
OAUTH_CLIENT_ID=commerce-backend-gyth8cvm
OAUTH_CLIENT_SECRET=gDFaUWuS6aV7WFuI11xZL3JiUm48W6FhIyqAU0UumqNqaFMPCWeexDdHE7M6lGXh
