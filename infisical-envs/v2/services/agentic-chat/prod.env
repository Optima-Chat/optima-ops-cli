# ==============================================================================
# optima-agentic-chat / prod
# Agentic Chat 服务 Production 配置
# 共享引用已移至 common.env，此文件只包含 prod 特有配置
# ==============================================================================

# ================================================
# Database Configuration (Prod 特有)
# ================================================
DB_USER=${prod.shared-secrets.database-users.CHAT_DB_USER}
DB_PASSWORD=${prod.shared-secrets.database-users.CHAT_DB_PASSWORD}
DB_NAME=${prod.shared-secrets.database-names.CHAT_DB_NAME}
REDIS_DB=${prod.shared-secrets.redis-databases.REDIS_DB_CHAT}

# 组合 URL（完整 Infisical 引用）
DATABASE_URL=postgresql://${prod.shared-secrets.database-users.CHAT_DB_USER}:${prod.shared-secrets.database-users.CHAT_DB_PASSWORD}@${common.shared-secrets.infrastructure.DATABASE_HOST}:${common.shared-secrets.infrastructure.DATABASE_PORT}/${prod.shared-secrets.database-names.CHAT_DB_NAME}
REDIS_URL=redis://${common.shared-secrets.infrastructure.REDIS_HOST}:${common.shared-secrets.infrastructure.REDIS_PORT}/${prod.shared-secrets.redis-databases.REDIS_DB_CHAT}

# ================================================
# MCP Host URL (Prod 特有)
# ================================================
MCP_HOST_URL=${prod.shared-secrets.domain-urls.MCP_HOST_INTERNAL_URL}

# ================================================
# Frontend URLs (Prod 特有)
# ================================================
NEXT_PUBLIC_BASE_URL=${prod.shared-secrets.domain-urls.AI_URL}
NEXT_PUBLIC_USER_AUTH_URL=${prod.shared-secrets.domain-urls.AUTH_URL}
NEXT_PUBLIC_COMMERCE_BACKEND_URL=${prod.shared-secrets.domain-urls.API_URL}
NEXT_PUBLIC_SHOP_DOMAIN=${prod.shared-secrets.domain-urls.SHOP_DOMAIN}

# ================================================
# Application Configuration (Prod 特有)
# ================================================
APP_SECRET=REPLACE_WITH_REAL_SECRET_KEY
EMAIL_PASSWORD_SECRET=m6JLPrZ4mdlyOIbLBWYOg2kOxuBsVQcmIzhsYh5pKcM=
NODE_ENV=production

# ================================================
# OAuth Configuration (Prod 特有)
# ================================================
NEXT_PUBLIC_OAUTH_CLIENT_ID=agentic-chat-tlioboop
