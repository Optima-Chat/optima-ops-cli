# ==============================================================================
# optima-agentic-chat / staging
# Agentic Chat 服务 Staging 配置
# 共享引用已移至 common.env，此文件只包含 staging 特有配置
# ==============================================================================

# ================================================
# Database Configuration (Staging 特有)
# ================================================
DB_USER=${staging.shared-secrets.database-users.CHAT_DB_USER}
DB_PASSWORD=${staging.shared-secrets.database-users.CHAT_DB_PASSWORD}
DB_NAME=${staging.shared-secrets.database-names.CHAT_DB_NAME}
REDIS_DB=${staging.shared-secrets.redis-databases.REDIS_DB_CHAT}

# 组合 URL（完整 Infisical 引用）
DATABASE_URL=postgresql://${staging.shared-secrets.database-users.CHAT_DB_USER}:${staging.shared-secrets.database-users.CHAT_DB_PASSWORD}@${common.shared-secrets.infrastructure.DATABASE_HOST}:${common.shared-secrets.infrastructure.DATABASE_PORT}/${staging.shared-secrets.database-names.CHAT_DB_NAME}
REDIS_URL=redis://${common.shared-secrets.infrastructure.REDIS_HOST}:${common.shared-secrets.infrastructure.REDIS_PORT}/${staging.shared-secrets.redis-databases.REDIS_DB_CHAT}

# ================================================
# MCP Host URL (Staging 特有)
# ================================================
MCP_HOST_URL=${staging.shared-secrets.domain-urls.MCP_HOST_INTERNAL_URL}

# ================================================
# Frontend URLs (Staging 特有)
# ================================================
NEXT_PUBLIC_BASE_URL=${staging.shared-secrets.domain-urls.AI_URL}
NEXT_PUBLIC_USER_AUTH_URL=${staging.shared-secrets.domain-urls.AUTH_URL}
NEXT_PUBLIC_COMMERCE_BACKEND_URL=${staging.shared-secrets.domain-urls.API_URL}
NEXT_PUBLIC_SHOP_DOMAIN=${staging.shared-secrets.domain-urls.SHOP_DOMAIN}

# ================================================
# Application Configuration (Staging 特有)
# ================================================
APP_SECRET=staging-REPLACE_WITH_REAL_SECRET_KEY
EMAIL_PASSWORD_SECRET=staging-m6JLPrZ4mdlyOIbLBWYOg2kOxuBsVQcmIzhsYh5pKcM=
NODE_ENV=staging

# ================================================
# OAuth Configuration (Staging 特有)
# ================================================
NEXT_PUBLIC_OAUTH_CLIENT_ID=agentic-chat-stage
