# ==============================================================================
# optima-mcp-host / prod
# MCP Host 服务 Production 配置
# 共享引用已移至 common.env，此文件只包含 prod 特有配置
# ==============================================================================

# ================================================
# Database Configuration (Prod 特有)
# ================================================
DB_USER=${prod.shared-secrets.database-users.MCP_DB_USER}
DB_PASSWORD=${prod.shared-secrets.database-users.MCP_DB_PASSWORD}
DB_NAME=${prod.shared-secrets.database-names.MCP_DB_NAME}
REDIS_DB=${prod.shared-secrets.redis-databases.REDIS_DB_MCP}

# 组合 URL（完整 Infisical 引用）
DATABASE_URL=postgresql://${prod.shared-secrets.database-users.MCP_DB_USER}:${prod.shared-secrets.database-users.MCP_DB_PASSWORD}@${common.shared-secrets.infrastructure.DATABASE_HOST}:${common.shared-secrets.infrastructure.DATABASE_PORT}/${prod.shared-secrets.database-names.MCP_DB_NAME}
REDIS_URL=redis://${common.shared-secrets.infrastructure.REDIS_HOST}:${common.shared-secrets.infrastructure.REDIS_PORT}/${prod.shared-secrets.redis-databases.REDIS_DB_MCP}

# ================================================
# URLs (Prod 特有)
# ================================================
USER_AUTH_URL=${prod.shared-secrets.domain-urls.AUTH_URL}
OAUTH_ISSUER=${prod.shared-secrets.domain-urls.AUTH_URL}
PUBLIC_BASE_URL=${prod.shared-secrets.domain-urls.MCP_URL}
CORS_ORIGINS=${prod.shared-secrets.domain-urls.AI_URL},${prod.shared-secrets.domain-urls.AUTH_URL},${prod.shared-secrets.domain-urls.API_URL},http://localhost:3000,http://localhost:8401

# ================================================
# Application Configuration (Prod 特有)
# ================================================
APP_NAME=mcp-host-prod
LOG_LEVEL=INFO
DEBUG=false
ENABLE_DEBUG_ENDPOINTS=false
MCP_CONFIG_PATH=configs/mcp-servers-ecs-prod.json

# ================================================
# OAuth & Session (Prod 特有)
# ================================================
OAUTH_CLIENT_ID=mcp-host-jnlqpq10
OAUTH_CLIENT_SECRET=OpbumiEzXmDOk3H2FVWEc5WjZqkr2GUifvQKrinxhxLFx9jedYLXgNV6iopPqIha
SESSION_SECRET=3LVX88dqXeKnZZY5GYTuNbAW7EFgmnMsFsnVl2AQgkG=
