# ==============================================================================
# optima-mcp-host / common
# MCP Host 服务跨环境共享配置（静态 + 共享引用）
# prod/staging 只需导入此文件 + 自己的环境特有配置
# ==============================================================================

# ================================================
# Application Configuration (静态)
# ================================================
HOST=0.0.0.0
PORT=8300

# ================================================
# Shared Infrastructure References (共享引用)
# ================================================
DATABASE_HOST=${common.shared-secrets.infrastructure.DATABASE_HOST}
DATABASE_PORT=${common.shared-secrets.infrastructure.DATABASE_PORT}
REDIS_HOST=${common.shared-secrets.infrastructure.REDIS_HOST}
REDIS_PORT=${common.shared-secrets.infrastructure.REDIS_PORT}

# ================================================
# LLM APIs (共享引用)
# ================================================
ANTHROPIC_API_KEY=${common.shared-secrets.third-party-apis.anthropic.ANTHROPIC_API_KEY}
OPENAI_API_KEY=${common.shared-secrets.third-party-apis.openai.OPENAI_API_KEY}

# ================================================
# Google Search (共享引用)
# ================================================
GOOGLE_API_KEY=${common.shared-secrets.third-party-apis.google.GOOGLE_API_KEY}
GOOGLE_SEARCH_CX=${common.shared-secrets.third-party-apis.google.GOOGLE_SEARCH_CX}
GOOGLE_CSE_ID=${common.shared-secrets.third-party-apis.google.GOOGLE_SEARCH_CX}

# ================================================
# Email Configuration (共享引用)
# ================================================
EMAIL_HOST=${common.shared-secrets.third-party-apis.resend.RESEND_SMTP_HOST}
EMAIL_PORT=${common.shared-secrets.third-party-apis.resend.RESEND_SMTP_PORT}
EMAIL_USER=${common.shared-secrets.third-party-apis.resend.RESEND_FROM_EMAIL}
EMAIL_PASS=${common.shared-secrets.third-party-apis.resend.RESEND_API_KEY}
EMAIL_FROM=${common.shared-secrets.third-party-apis.resend.RESEND_FROM_NAME}
EMAIL_SECURE=true

# ================================================
# Cache Configuration (静态)
# ================================================
CACHE_ENABLED=true

# ================================================
# Workspace Configuration (静态)
# ================================================
WORKSPACE_ROOT=/data/workspaces
WORKSPACE_CONTAINER_IMAGE=mcp-workspace
WORKSPACE_IMAGE_TAG=latest
WORKSPACE_MEMORY_LIMIT=2147483648
WORKSPACE_CPU_LIMIT=1.0
WORKSPACE_PIDS_LIMIT=100
WORKSPACE_DEFAULT_QUOTA=10737418240
WORKSPACE_MAX_FILE_SIZE=1073741824

# ================================================
# Docker Configuration (静态)
# ================================================
DOCKER_GID=993
