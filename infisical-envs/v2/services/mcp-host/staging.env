# ==============================================================================
# optima-mcp-host / staging
# MCP Host 服务 Staging 配置
# 共享引用已移至 common.env，此文件只包含 staging 特有配置
# ==============================================================================

# ================================================
# Database Configuration (Staging 特有)
# ================================================
DB_USER=${staging.shared-secrets.database-users.MCP_DB_USER}
DB_PASSWORD=${staging.shared-secrets.database-users.MCP_DB_PASSWORD}
DB_NAME=${staging.shared-secrets.database-names.MCP_DB_NAME}
REDIS_DB=${staging.shared-secrets.redis-databases.REDIS_DB_MCP}

# 组合 URL（完整 Infisical 引用）
DATABASE_URL=postgresql://${staging.shared-secrets.database-users.MCP_DB_USER}:${staging.shared-secrets.database-users.MCP_DB_PASSWORD}@${common.shared-secrets.infrastructure.DATABASE_HOST}:${common.shared-secrets.infrastructure.DATABASE_PORT}/${staging.shared-secrets.database-names.MCP_DB_NAME}
REDIS_URL=redis://${common.shared-secrets.infrastructure.REDIS_HOST}:${common.shared-secrets.infrastructure.REDIS_PORT}/${staging.shared-secrets.redis-databases.REDIS_DB_MCP}

# ================================================
# URLs (Staging 特有)
# ================================================
USER_AUTH_URL=${staging.shared-secrets.domain-urls.AUTH_URL}
OAUTH_ISSUER=${staging.shared-secrets.domain-urls.AUTH_URL}
PUBLIC_BASE_URL=${staging.shared-secrets.domain-urls.MCP_URL}
CORS_ORIGINS=${staging.shared-secrets.domain-urls.AI_URL},${staging.shared-secrets.domain-urls.AUTH_URL},${staging.shared-secrets.domain-urls.API_URL},http://localhost:3000,http://localhost:8401

# ================================================
# Application Configuration (Staging 特有)
# ================================================
APP_NAME=mcp-host-stage
LOG_LEVEL=DEBUG
DEBUG=true
ENABLE_DEBUG_ENDPOINTS=true
MCP_CONFIG_PATH=configs/mcp-servers-ecs-stage.json

# ================================================
# OAuth & Session (Staging 特有)
# ================================================
OAUTH_CLIENT_ID=mcp-host-stage
OAUTH_CLIENT_SECRET=staging-OpbumiEzXmDOk3H2FVWEc5WjZqkr2GUifvQKrinxhxLFx9jedYLXgNV6iopPqIha
SESSION_SECRET=staging-3LVX88dqXeKnZZY5GYTuNbAW7EFgmnMsFsnVl2AQgkG=
