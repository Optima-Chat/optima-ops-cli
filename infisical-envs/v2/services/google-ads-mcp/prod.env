# ==============================================================================
# optima-google-ads-mcp / prod
# Google Ads MCP 服务 Production 配置
# 共享引用已移至 common.env，此文件只包含 prod 特有配置
# ==============================================================================

# ================================================
# Container Configuration (Prod 特有)
# ================================================
CONTAINER_NAME_MAIN=optima-google-ads-mcp-prod
CONTAINER_NAME_BILLING=optima-google-ads-billing-checker-prod

# ================================================
# Runtime Configuration (Prod 特有)
# ================================================
LOG_LEVEL=INFO
ENVIRONMENT=production

# ================================================
# Database Configuration (Prod 特有)
# ================================================
DB_USER=${prod.shared-secrets.database-users.GOOGLE_ADS_DB_USER}
DB_PASSWORD=${prod.shared-secrets.database-users.GOOGLE_ADS_DB_PASSWORD}
DB_NAME=${prod.shared-secrets.database-names.GOOGLE_ADS_DB_NAME}

# 组合 URL（完整 Infisical 引用）
DATABASE_URL=postgresql+asyncpg://${prod.shared-secrets.database-users.GOOGLE_ADS_DB_USER}:${prod.shared-secrets.database-users.GOOGLE_ADS_DB_PASSWORD}@${common.shared-secrets.infrastructure.DATABASE_HOST}:${common.shared-secrets.infrastructure.DATABASE_PORT}/${prod.shared-secrets.database-names.GOOGLE_ADS_DB_NAME}

# ================================================
# Backend Service URLs (Prod 特有)
# ================================================
COMMERCE_API_URL=${prod.shared-secrets.domain-urls.API_URL}
USER_AUTH_URL=${prod.shared-secrets.domain-urls.AUTH_URL}
PUBLIC_URL=${prod.shared-secrets.domain-urls.ADS_MCP_URL}

# ================================================
# Proxy Configuration (Prod 特有)
# ================================================
SERVICE_HTTP_PROXY=

# ================================================
# Optional: OpenAI (Prod 特有)
# ================================================
OPENAI_API_KEY=
