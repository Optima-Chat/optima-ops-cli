# ==============================================================================
# optima-google-ads-mcp / staging
# Google Ads MCP 服务 Staging 配置
# 共享引用已移至 common.env，此文件只包含 staging 特有配置
# ==============================================================================

# ================================================
# Container Configuration (Staging 特有)
# ================================================
CONTAINER_NAME_MAIN=optima-google-ads-mcp-stage
CONTAINER_NAME_BILLING=optima-google-ads-billing-checker-stage

# ================================================
# Runtime Configuration (Staging 特有)
# ================================================
LOG_LEVEL=DEBUG
ENVIRONMENT=staging

# ================================================
# Database Configuration (Staging 特有)
# ================================================
DB_USER=${staging.shared-secrets.database-users.GOOGLE_ADS_DB_USER}
DB_PASSWORD=${staging.shared-secrets.database-users.GOOGLE_ADS_DB_PASSWORD}
DB_NAME=${staging.shared-secrets.database-names.GOOGLE_ADS_DB_NAME}

# 组合 URL（完整 Infisical 引用）
DATABASE_URL=postgresql+asyncpg://${staging.shared-secrets.database-users.GOOGLE_ADS_DB_USER}:${staging.shared-secrets.database-users.GOOGLE_ADS_DB_PASSWORD}@${common.shared-secrets.infrastructure.DATABASE_HOST}:${common.shared-secrets.infrastructure.DATABASE_PORT}/${staging.shared-secrets.database-names.GOOGLE_ADS_DB_NAME}

# ================================================
# Backend Service URLs (Staging 特有)
# ================================================
COMMERCE_API_URL=${staging.shared-secrets.domain-urls.API_URL}
USER_AUTH_URL=${staging.shared-secrets.domain-urls.AUTH_URL}
PUBLIC_URL=${staging.shared-secrets.domain-urls.ADS_MCP_URL}

# ================================================
# Proxy Configuration (Staging 特有)
# ================================================
SERVICE_HTTP_PROXY=

# ================================================
# Optional: OpenAI (Staging 特有)
# ================================================
OPENAI_API_KEY=
